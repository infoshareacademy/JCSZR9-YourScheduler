@using YourScheduler.BusinessLogic.Models.DTOs;
@model TeamDto

@{
    ViewData["Title"] = "AddThisTeam";
}

<h3>Czy chcesz dodać ten zespół?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            <label asp-for="Name">Nazwa zespołu</label>
        </dt>
        <dd class="col-sm-10">
            @Model.Name
        </dd>
        <dt class="col-sm-2">
            <label asp-for="Description">Opis zespołu</label>
        </dt>
        <dd class="col-sm-10">
            @Model.Description
        </dd>
                   
    </dl>

    <form asp-action="AddThisTeam">
        <input type="submit" value="Dodaj zespół" class="btn btn-primary" /> |
        <a asp-controller="Team" asp-action="GetAllTeams" class="btn btn-primary">Powrót do listy zespołow</a>
    </form>
</div>
